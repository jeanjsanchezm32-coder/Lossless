name: Compile Submissions

on:
  workflow_run:
    workflows: ["Validate & Auto Merge"]
    types:
      - completed
  push:
    branches:
      - main
    paths:
      - 'submissions/**'

permissions:
  contents: write

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.PAT_TOKEN }}

      - name: Compile JSON Submissions
        run: |
          python3 .github/scripts/compile.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add music.json submissions/
          
          # Only commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: compile new submissions to music.json [skip ci]"
            git pull --rebase origin main
            git push
          fi
